---
import { SITE } from '@config/constants'
import SocialLinks from './SocialLinks.astro'
---

<section class="relative min-h-screen flex items-center justify-center overflow-hidden">
  <!-- Blue-pink gradient atmosphere -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none" aria-hidden="true">
    <!-- Top-right pink blob -->
    <div class="absolute -top-[15%] -right-[15%] w-[80vw] h-[80vw] md:w-[55vw] md:h-[55vw] rounded-full opacity-80 md:opacity-70 bg-rose-300 dark:bg-rose-400/70 blur-[80px] md:blur-[140px] animate-float" />
    <!-- Bottom-left blue blob -->
    <div class="absolute top-[35%] -left-[15%] w-[75vw] h-[75vw] md:w-[45vw] md:h-[45vw] rounded-full opacity-80 md:opacity-70 bg-sky-300 dark:bg-sky-400/70 blur-[80px] md:blur-[120px] animate-float-delay" />
    <!-- Center purple accent -->
    <div class="absolute top-[20%] left-[20%] w-[60vw] h-[60vw] md:w-[35vw] md:h-[35vw] rounded-full opacity-70 md:opacity-60 bg-accent-300 dark:bg-accent-400/60 blur-[70px] md:blur-[100px] animate-float-slow" />
  </div>

  <!-- Content -->
  <div class="relative z-10 w-full max-w-xl mx-auto px-5">
    <!-- Profile Card -->
    <div class="animate-fade-up rounded-3xl bg-white/40 dark:bg-slate-800/50 backdrop-blur-xl border border-white/40 dark:border-white/10 shadow-xl p-5 sm:p-7 transition-colors duration-700">
      <div class="flex items-start gap-5 sm:gap-6">
        <!-- Avatar with gradient border -->
        <div class="w-[88px] h-[88px] sm:w-[104px] sm:h-[104px] rounded-[18px] sm:rounded-[22px] bg-gradient-to-tr from-rose-400 via-accent-400 to-sky-400 p-1 shadow-lg flex-shrink-0">
          <img src="/images/avatar.jpg" alt={SITE.name} class="w-full h-full rounded-[14px] sm:rounded-[18px] object-cover bg-white dark:bg-slate-800" />
        </div>

        <!-- Name + Bio -->
        <div class="flex-1 min-w-0 pt-1">
          <span class="inline-block text-xs font-medium tracking-[0.3em] text-accent-500 mb-1">
            秋
          </span>
          <h1 class="text-xl sm:text-2xl font-bold text-slate-900 dark:text-white mb-1.5 leading-snug tracking-wider truncate">
            {SITE.name}
          </h1>
          <p class="text-[13px] sm:text-sm text-slate-600 dark:text-slate-300 leading-relaxed">
            {SITE.description}
          </p>
        </div>
      </div>

      <!-- Social Links -->
      <div class="mt-6 pt-5 border-t border-black/5 dark:border-white/5">
        <SocialLinks />
      </div>
    </div>
  </div>

  <!-- Scroll indicator -->
  <div class="absolute bottom-10 left-1/2 -translate-x-1/2 flex flex-col items-center gap-2 text-ink-300/60 dark:text-ink-inverted/20">
    <span class="text-[10px] uppercase tracking-[0.3em]">Scroll</span>
    <svg class="w-4 h-4 animate-bounce" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1">
      <path stroke-linecap="round" stroke-linejoin="round" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
    </svg>
  </div>
</section>
